<% title "#{@pgbranch.branch_name}" %>
<div id="content" class="container_16">
    	<!-- begin main content area -->
        <div id="page_content" class="grid_11 alpha">
        	<h3 class="content_title"> <%=@pgbranch.branch_name %></h3>

<% if !flash[:notice].blank? %>
<div>
	<p align = "center"><%= flash[:notice] %></p>
</div>
<% end %>        	
  <table border='border'>
    <tr>
      <th>Branch Name:</th>
      <td><%= @pgbranch.branch_name %></td>
    </tr>
    
    <tr>
    	<th>Minimum CGPA for Dream Option</th>
    	<td><%= @pgbranch.dream_option %></td>
    <tr>
      <th>Created At:</th>
      <td><%= @pgbranch.created_at %></td>
    </tr>
    <tr>
      <th>Updated At:</th>
      <td><%= @pgbranch.updated_at %></td>
    </tr>
  </table>
<br/>
<p align="right"><%= link_to("Back", {:action => 'index'}) %>&nbsp;&nbsp;&nbsp;<%= link_to("Edit", {:action => 'edit', :id=> @pgbranch.id }) %></p>

</div>
</div> 