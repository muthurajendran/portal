<div id="content" class="container_16">
    	<!-- begin main content area -->
        <div id="page_content" class="grid_11 alpha">
        	<h3 class="content_title">Add <%= @student.name %> to Database</h3>
        	
        	
 <% if !flash[:notice].blank? %>
<div>
	<p align = "center"><%= flash[:notice] %></p>
</div>
<% end %> 

<div>
   <%= form_for(:student, :url => {:action => 'create', :id => @student.id}) do |f| -%>
</div>
 <p align = "center">Add  <%= @student.name %> to the database</p>
	<div class = "actions" >
  		<%= submit_tag("Delete") %>
  	</div>
<% end %>
 <div>
    	<p align="right"><%= link_to("Click to go Back", {:action => 'index'}) %><br/><br/></p>
    </div>
</div>
</div>